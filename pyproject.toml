[project]
name = "wpi"
version = "0.1.0"
description = "Turn-based country simulation engine"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.2",
    "pydantic>=2.12.5",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.15.0",
]
